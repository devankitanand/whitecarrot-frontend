# API Base URL
# For development: leave empty or use '/api' to use Next.js proxy (default)
# For production with same domain: use '/api' 
# For production with different domain: use full URL like 'https://api.yourdomain.com'
NEXT_PUBLIC_API_BASE_URL=
